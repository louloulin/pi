# lum1457-chatinput-audit-conpty-80x26.png — chars, 80x26 cells (Windows ConPTY backend)

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [1] 1. idle composer at 80x26  (cursor 80,25, frame f1f891b9f4eb, px f5ed827cb079, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> type a prompt — /help for commands
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [2] 2. three words typed; caret at the end of the draft  (cursor 36,24, frame b046f43559b8, px 96936087c7d9, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha beta gamma▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma▍'  — a plain draft renders with the block caret at the end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [3] 3. Ctrl+W deletes one word backward (codex, Martty and pi-ts all bind this)  (cursor 19,24, frame 0d408e573f5c, px e142a0ccf1dd, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha beta ▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta ▍'  — `gamma` is gone and the caret sits where it was

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [4] 4. Alt+B hops one word left (esc-b; untouched by the LUM-1360 defect)  (cursor 14,24, frame af3f99bcd3d8, px 6dcea6e575cf, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha ▍beta
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha ▍beta'  — the caret stops at the start of `beta`; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [5] 5. Alt+F hops one word right — the LUM-1360 fix (before: this forked the session)  (cursor 13,24, frame 09c70e77e7c5, px 1df12ddc8099, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha beta▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   reject       '/fork: no session database'  — the fork picker must NOT run: `alt+f` is the editor's forward-word chord, not `app.session.fork` (LUM-1360). This row is a hard FAIL, so the pre-fix binary fails this scenario.
  assert PASS   probe        '> alpha beta▍'  — the caret is back at the end of `beta`, one word right; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [6] 6. Ctrl+A / Ctrl+E are line-scoped, not draft-scoped  (cursor 19,24, frame 3150bcec6bf0, px 210e8e6b1afe, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> ▍alpha beta gamma
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> ▍alpha beta gamma'  — Ctrl+A put the caret at the start of the only line

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [7] 7. Ctrl+K kills to the end of the line and Ctrl+Y yanks it back  (cursor 20,24, frame 7749ba4b1134, px dd5e1f06e8b7, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha beta gamma ▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma'  — the kill ring round-trips the whole killed tail

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [8] 8. Ctrl+J opens a second draft row instead of submitting  (cursor 31,24, frame ecbf4f17cc30, px f4cc265cd104, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> alpha beta gamma second line▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma'  — line 1 keeps its content (Ctrl+J did not submit)
  assert XFAIL  probe        '  second line▍'  — line 2 is a real draft row with the caret at its end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [9] 9. double-click inside the composer selects the word under the pointer  (cursor 63,24, frame 3cea96b3ec7d, px 0ec531c95131, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them





















> alpha beta gamma second
  line<MPress:9,23><MRelease:9,23><MPress:9,23><MRelease:9,23>▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft is intact after the double click

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [10] 10. a drag that starts in the composer extends the selection as the pointer moves left  (cursor 63,24, frame b54c6e038ba5, px 1a0169d372a5, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















> alpha beta gamma second
  line<MPress:9,23><MRelease:9,23><MPress:9,23><MRelease:9,23><MPress:20,23><MDr
  ag:12,23><MDrag:6,23>▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft survives a drag that leaves the composer body

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [11] 11. Ctrl+C clears the draft on the first press (no exit)  (cursor 36,24, frame f1f891b9f4eb, px f5ed827cb079, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> type a prompt — /help for commands
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> type a prompt'  — the composer is back to its placeholder and the process is alive

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [12] 12. Enter submits, the transcript echoes the prompt  (cursor 30,3, frame 7192501fd11d, px a390a925b549, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  f          hello




















> type a prompt — /help for commands
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity'  — the submitted prompt is echoed in the transcript

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [13] 13. Up on the empty composer recalls it from the prompt history  (cursor 30,3, frame 7dc8a7c6815e, px 0f6ef83aae87, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  f          hello




















> hello codex parity▍
Faux test model  session-18d7cb9a5fe113c8          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity▍'  — history recall puts the prompt back in the composer

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [14] 14. Ctrl+R opens the reverse search over the prompt history  (cursor 30,3, frame dc486e6b6380, px 149c90398645, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  f          hello




















> hello codex parity▍
Faux test model ?/8.2k  reverse-i-search: hel  Enter accept · Esc/Ctrl+C cancel…
  assert PASS   probe        'hello codex parity'  — the reverse search lists the matching history entry
