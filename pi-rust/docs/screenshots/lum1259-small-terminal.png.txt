# lum1259-small-terminal.png — chars, 80x24 cells

===== LUM-1259 tip 4b187b418 on a standard 80x24 terminal  |  [1] 1. startup on 80x24 — the header legend vs the transcript area  (cursor 80,23) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or exten

> type a prompt — /help for commands
Faux test model  session-18d71fa18f9a8c29          in 0 out 0 ?/8.2k  ? for help

===== LUM-1259 tip 4b187b418 on a standard 80x24 terminal  |  [2] 2. /help on 80x24  (cursor 80,21) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or exten
> selector / cancel turn                                                       ┃
> type a prompt — /help for commands
Faux test model  session-18d71fa18f9a8c29          in 0 out 0 ?/8.2k  ? for help

===== LUM-1259 tip 4b187b418 on a standard 80x24 terminal  |  [3] 3. a real prompt, then Enter, on 80x24  (cursor 25,21) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or exten
  faux-model (faux) hello                                                      ┃
> type a prompt — /help for commands
Faux test model  session-18d71fa18f9a8c29          in 0 out 0 ?/8.2k  ? for help

===== LUM-1259 tip 4b187b418 on a standard 80x24 terminal  |  [4] 4. Alt+H (the header's own advertised workaround) on 80x24  (cursor 80,23) =====
> slash commands: /help show this help text /clear clear the message view /new
> start a new session /copy copy the last assistant message to the clipboard
> /name [name] show or set the session display name /model pick a model (opens
> selector) /session show the current session info /export [path] export the
> session (HTML, or JSONL for a .jsonl path) /resume resume a previous session
> /tree navigate the session tree and switch branches /fork branch a new session
> from a user message /clone copy the current session into a new session file
> /settings show or change interface settings /thinking set the reasoning level
> (/thinking off|minimal|low|medium|high|xhigh|max) /trust show or set project
> trust (/trust yes|no) /compact summarize the conversation prefix to free
> context /hotkeys list the keyboard shortcuts /exit quit the interactive
> session keys: Enter submit prompt Up / Down navigate prompt history PgUp/PgDn
> scroll the chat log one page Home / End jump to the start / end of the chat
> log Ctrl+C abort the current turn (or exit on idle) Ctrl+D exit on an empty
> prompt Ctrl+L open the model selector Ctrl+U clear the prompt buffer Esc close
> selector / cancel turn
> hello
  faux-model (faux) hello




> type a prompt — /help for commands
Faux test model  session-18d71fa18f9a8c29  in 0 out 0 ?/8.2k  Startup header: co
