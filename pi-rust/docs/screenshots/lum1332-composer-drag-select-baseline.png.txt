# lum1332-composer-drag-select-baseline.png — chars, 100x20 cells

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [1] 1. a drag across 'llo wo' highlights nothing (pre-fix)  (cursor 36,18, frame f39a6693fffe, px 28bc18deed06, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
















> he▍llo world
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> he▍llo world' (x1)  — the drag moved nothing: the caret is still on the character LUM-1327's press placed it on
  assert PASS   expect       '> hello world▍' (x0)  — and it did not follow the drag to the end either
  assert PASS   expect       '# reverse' (x0)  — no cell of the frame is highlighted
  assert PASS   expect       '# clipboard' (x0)  — and nothing was copied

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [2] 2. the release copies nothing (pre-fix)  (cursor 36,18, frame f39a6693fffe, px 28bc18deed06, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
















> he▍llo world
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '# clipboard' (x0)  — the release of a drag that never selected has nothing to copy
  assert PASS   expect       '> he▍llo world' (x1)  — one caret, exactly where the press left it

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [3] 3. the click still places the caret — LUM-1327, already fixed  (cursor 36,18, frame f39a6693fffe, px 28bc18deed06, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
















> he▍llo world
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> he▍llo world'
  assert PASS   expect       '# reverse' (x0)  — and the click still starts no selection

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [4] 4. a drag across a line break highlights nothing (pre-fix)  (cursor 14,18, frame f6d6d7554640, px 1f43e8f83d3e, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them















> f▍irst line
  second line
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> f▍irst line'
  assert PASS   expect       '  second line' (x1)  — the second row is unhighlighted and carries no caret
  assert PASS   expect       '# reverse' (x0)  — no row is highlighted
  assert PASS   expect       '# clipboard' (x0)  — and nothing is copied while dragging

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [5] 5. the release copies nothing (pre-fix)  (cursor 14,18, frame f6d6d7554640, px 1f43e8f83d3e, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them















> f▍irst line
  second line
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '# clipboard' (x0)  — a cross-row drag selects nothing to copy
  assert PASS   expect       '> f▍irst line' (x1)  — the caret is still on the pressed character

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [6] 6. a drag that leaves the composer highlights nothing (pre-fix)  (cursor 9,17, frame bb19b94b9c88, px 4fb7a713f8ef, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them















> first ▍line
  second line
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '# reverse' (x0)  — neither the composer nor the transcript is highlighted
  assert PASS   expect       '> first ▍line' (x1)  — the caret is where the press put it

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [7] 7. the release copies nothing (pre-fix)  (cursor 9,17, frame bb19b94b9c88, px 4fb7a713f8ef, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them















> first ▍line
  second line
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '# clipboard' (x0)  — no release ever queues a clipboard write
  assert PASS   expect       '> first ▍line' (x1)  — and the caret still has not moved
  assert PASS   expect       'Faux test model'

===== LUM-1332 — the same pointer gestures against the pre-fix binary (100x20, real PTY). Panels 1-2: dragging across 'llo wo' highlights nothing, moves nothing and copies nothing — the composer consumed the drag and the release and threw both away, leaving only the caret LUM-1327's press placed on the pressed character. Panel 3: the same cell clicked — the click (LUM-1327) is what places the caret, so this panel is byte-identical to panels 1-2 on purpose. Panels 4-5: a drag across a line break likewise selects nothing and copies nothing. Panels 6-8: a drag that leaves the composer selects nothing, its release copies nothing, and a press outside has no highlight to dismiss. The '# reverse' / '# clipboard' evidence lines the harness would print for a selection are absent from every panel. This is the state lum1332-composer-drag-select.json fixes.  |  [8] 8. a press outside the composer dismisses nothing (pre-fix)  (cursor 9,17, frame bb19b94b9c88, px 4fb7a713f8ef, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them















> first ▍line
  second line
Faux test model  session-18d78d8e63a4a6f0                              in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '# clipboard' (x0)  — no click ever queues a clipboard write
  assert PASS   expect       '# reverse' (x0)  — and there is no highlight to dismiss: nothing was ever selected
  assert PASS   expect       '> first ▍line' (x1)  — the caret is still where the press put it
  assert PASS   expect       'Faux test model'
