# before-reload.png — chars, 120x34 cells

===== LUM-1306 — the A side: the *same* prepared HOME and the *same* keystrokes against the pre-change binary (origin/feature/pi.rs tip 4208bd843). Panels 1-3 are unchanged; panel 4 is the parser rejecting the command that did not exist yet.  |  [1] 1. idle 120x34 — default palette; overrides on disk, nothing applied yet  (cursor 120,33, frame 04bbe30e21c0, px fa880d6088af, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.











> type a prompt — /help for commands
Faux test model  session-18d742815b46b275                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'pi v0.1.0'
  assert PASS   expect       'Faux test model'

===== LUM-1306 — the A side: the *same* prepared HOME and the *same* keystrokes against the pre-change binary (origin/feature/pi.rs tip 4208bd843). Panels 1-3 are unchanged; panel 4 is the parser rejecting the command that did not exist yet.  |  [2] 2. /clear + /hotkeys + Home — the on-disk override is live before any reload (Ctrl+P)  (cursor 120,31, frame 11d45da60e56, px 71abdaf6c78a, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
· keyboard shortcuts:                                                                                                  ┃
·                                                                                                                      ┃
· navigation:                                                                                                          │
·   Ctrl+P           previous line / prompt history                                                                    │
·   Down             next line / prompt history                                                                        │
·   Left / Ctrl+B    move cursor left                                                                                  │
·   Right / Ctrl+F   move cursor right                                                                                 │
·   Alt+Left / Ctrl+Left / Alt+B move by word (left)                                                                   │
·   Alt+Right / Ctrl+Right / Alt+F move by word (right)                                                                │
·   Home / Ctrl+Home / Ctrl+A start of line                                                                            │
·   End / Ctrl+End / Ctrl+E end of line     ↓ Jump to latest message · End                                             │
> type a prompt — /help for commands
Faux test model  session-18d742815b46b275                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'previous line / prompt history'
  assert PASS   expect       'Ctrl+P'

===== LUM-1306 — the A side: the *same* prepared HOME and the *same* keystrokes against the pre-change binary (origin/feature/pi.rs tip 4208bd843). Panels 1-3 are unchanged; panel 4 is the parser rejecting the command that did not exist yet.  |  [3] 3. /clear + /help + Home — the head of the command list  (cursor 43,31, frame ef12d087279e, px 12bf65b86f23, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
· slash commands:                                                                                                      ┃
·   /help     show this help text                                                                                      ┃
·   /clear    clear the message view                                                                                   ┃
·   /new      start a new session                                                                                      ┃
·   /copy     copy the last assistant message to the clipboard                                                         │
·   /name [name] show or set the session display name                                                                  │
·   /model    pick a model (opens selector)                                                                            │
·   /session  show the current session info                                                                            │
·   /export [path] export the session (HTML, or JSONL for a .jsonl path)                                               │
·   /resume   resume a previous session                                                                                │
·   /tree     navigate the session tree and ↓ Jump to latest message · End                                             │
> type a prompt — /help for commands
Faux test model  session-18d742815b46b275                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'slash commands:'
  assert PASS   expect       '/clear'
  assert PASS   expect       '/model'

===== LUM-1306 — the A side: the *same* prepared HOME and the *same* keystrokes against the pre-change binary (origin/feature/pi.rs tip 4208bd843). Panels 1-3 are unchanged; panel 4 is the parser rejecting the command that did not exist yet.  |  [4] 4. /reload on the pre-change binary — the parser does not know the command  (cursor 120,31, frame f72834cc9384, px 0f2438bb316e, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> unknown command /reload — try /help










> type a prompt — /help for commands
Faux test model  session-18d742815b46b275                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'unknown command /reload'
