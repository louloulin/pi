# lum1241-turn-and-commands.png — chars, 120x34 cells

===== LUM-1241 tip af3aa30a4  |  [1] 1. Ctrl+U, /model + Enter -> model selector overlay (faux + builtin providers)  (cursor 43,31) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.

Pick a model
────────────────────────────────────────
❯ Ling 2.6 1T                     ant-ling
  Ling 2.6 Flash                  ant-ling
  Ring 2.6 1T                     ant-ling
  Claude Haiku 4.5                anthropic
  Claude Opus 4.5                 anthropic
  Claude Sonnet 4.5               anthropic
> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [2] 2. Esc closes the selector, composer is restored  (cursor 43,31) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.









> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [3] 3. a real turn through the faux provider streams into the transcript  (cursor 25,24) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> hi
  faux-model (faux) hello







> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [4] 4. !local bash runs in-process and its output lands in the transcript  (cursor 11,28) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> hi
  faux-model (faux) hello
* bash echo pty-harness-ok && pwd
* pty-harness-ok
* /tmp/pi-pty-cwd-26642
* Took 0.1s



> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [5] 5. /help renders the implemented command surface + key list  (cursor 120,31) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> assistant message to the clipboard /name [name] show or set the session display name /model pick a model (opens      │
> selector) /session show the current session info /export [path] export the session (HTML, or JSONL for a .jsonl path)│
> /resume resume a previous session /tree navigate the session tree and switch branches /fork branch a new session from│
> a user message /clone copy the current session into a new session file /settings show or change interface settings   │
> /trust show or set project trust (/trust yes|no) /compact summarize the conversation prefix to free context /hotkeys ┃
> list the keyboard shortcuts /exit quit the interactive session keys: Enter submit prompt Up / Down navigate prompt   ┃
> history PgUp/PgDn scroll the chat log one page Home / End jump to the start / end of the chat log Ctrl+C abort the   ┃
> current turn (or exit on idle) Ctrl+D exit on an empty prompt Ctrl+L clear the screen Ctrl+U clear the prompt buffer ┃
> Esc close selector / cancel turn                                                                                     ┃
> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [6] 6. /hotkeys renders the keybinding table  (cursor 78,31) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> Ctrl+C clear the prompt (twice: exit) Ctrl+D exit when the prompt is empty Ctrl+Z suspend to the background Ctrl+P   │
> cycle to the next model Shift+Ctrl+P cycle to the previous model Ctrl+X copy the last assistant message Alt+Enter    │
> queue a follow-up message Alt+Up restore queued messages to the editor Ctrl+T show or hide thinking blocks Ctrl+O    │
> expand or collapse tool output (Ctrl+O by default) Alt+H expand or collapse the startup header (Alt+H by default)    │
> Ctrl+L open the model selector Alt+N start a new session Alt+T open the session tree Alt+F fork a session from a     │
> message Alt+R resume a session Ctrl+V attach a clipboard image (falls back to pasting text) selectors and completion:│
> Up move the selection up Down move the selection down PgUp selection page up PgDn selection page down Enter confirm  │
> the selection Esc / Ctrl+C close the selection list Tab path completion / accept autocomplete commands: / slash      ┃
> commands (/help, /model, /hotkeys, …) Esc close the selector / overlay first                                         ┃
> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [7] 7. PgUp scrolls the transcript back to the head of the /hotkeys message  (cursor 120,31) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> /resume resume a previous session /tree navigate the session tree and switch branches /fork branch a new session from│
> a user message /clone copy the current session into a new session file /settings show or change interface settings   │
> /trust show or set project trust (/trust yes|no) /compact summarize the conversation prefix to free context /hotkeys ┃
> list the keyboard shortcuts /exit quit the interactive session keys: Enter submit prompt Up / Down navigate prompt   ┃
> history PgUp/PgDn scroll the chat log one page Home / End jump to the start / end of the chat log Ctrl+C abort the   │
> current turn (or exit on idle) Ctrl+D exit on an empty prompt Ctrl+L clear the screen Ctrl+U clear the prompt buffer │
> Esc close selector / cancel turn                                                                                     │
> keyboard shortcuts: navigation: Up previous line / prompt history Down next line / prompt history Left / Ctrl+B move │
> cursor left Right / Ctrl+F move cursor right Alt+Left / Ctrl+Left / Alt+B move by word (left) Alt+Right / Ctrl+Right │
> type a prompt — /help for commands
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help

===== LUM-1241 tip af3aa30a4  |  [8] 8. known gap: # has no provider behind it, so no candidates appear  (cursor 36,32) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+Z to suspend
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  Ctrl+G for external editor
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> /resume resume a previous session /tree navigate the session tree and switch branches /fork branch a new session from│
> a user message /clone copy the current session into a new session file /settings show or change interface settings   │
> /trust show or set project trust (/trust yes|no) /compact summarize the conversation prefix to free context /hotkeys ┃
> list the keyboard shortcuts /exit quit the interactive session keys: Enter submit prompt Up / Down navigate prompt   ┃
> history PgUp/PgDn scroll the chat log one page Home / End jump to the start / end of the chat log Ctrl+C abort the   │
> current turn (or exit on idle) Ctrl+D exit on an empty prompt Ctrl+L clear the screen Ctrl+U clear the prompt buffer │
> Esc close selector / cancel turn                                                                                     │
> keyboard shortcuts: navigation: Up previous line / prompt history Down next line / prompt history Left / Ctrl+B move │
> cursor left Right / Ctrl+F move cursor right Alt+Left / Ctrl+Left / Alt+B move by word (left) Alt+Right / Ctrl+Right │
> #▍
Faux test model  session-18d711c78d55419a                                                  in 0 out 0 ?/8.2k  ? for help
