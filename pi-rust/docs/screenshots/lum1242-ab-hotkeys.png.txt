# lum1242-ab-hotkeys.png — chars, 100x34 cells
#
# BEFORE = origin/feature/pi.rs @ b435e2dbc (LUM-1245's Ctrl+L / wired-axis fix already merged)
# AFTER  = work/LUM-1242 @ this commit (this round's delta)
# binary = pi-rust/target/debug/pi, --model faux/faux-model, TERM=xterm-256color, PI_LANG=en
# harness = pty.fork + pyte (deliberately not committed: pyte/Pillow must never gate the build)

===== [1] BEFORE — the app: group wraps without any Shift+Tab entry (17 rows) ========

pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach
Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> autocomplete / abort streaming Ctrl+C clear the prompt (twice: exit) Ctrl+D exit when the prompt │
> is empty Ctrl+P cycle to the next model Shift+Ctrl+P cycle to the previous model Ctrl+X copy the │
> last assistant message Alt+Enter queue a follow-up message Alt+Up restore queued messages to the │
> editor Ctrl+T show or hide thinking blocks Ctrl+O expand or collapse tool output (Ctrl+O by      │
> default) Alt+H expand or collapse the startup header (Alt+H by default) Ctrl+L open the model    │
> selector Alt+N start a new session Alt+T open the session tree Alt+F fork a session from a messag│
> Alt+R resume a session Ctrl+V attach a clipboard image (falls back to pasting text) selectors and┃
> completion: Up move the selection up Down move the selection down PgUp selection page up PgDn    ┃
> selection page down Enter confirm the selection Esc / Ctrl+C close the selection list Tab path   ┃
> completion / accept autocomplete commands: / slash commands (/help, /model, /hotkeys, …) Esc clos┃
> the selector / overlay first                                                                     ┃
> type a prompt — /help for commands
Faux test model  session-18d7167fb19bca05                              in 0 out 0 ?/8.2k  ? for help


===== [2] AFTER — the app: group carries `Shift+Tab cycle the thinking level` (18 rows) ========

pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach
Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> autocomplete / abort streaming Ctrl+C clear the prompt (twice: exit) Ctrl+D exit when the prompt │
> is empty Shift+Tab cycle the thinking level Ctrl+P cycle to the next model Shift+Ctrl+P cycle to │
> the previous model Ctrl+X copy the last assistant message Alt+Enter queue a follow-up message    │
> Alt+Up restore queued messages to the editor Ctrl+T show or hide thinking blocks Ctrl+O expand or│
> collapse tool output (Ctrl+O by default) Alt+H expand or collapse the startup header (Alt+H by   │
> default) Ctrl+L open the model selector Alt+N start a new session Alt+T open the session tree    │
> Alt+F fork a session from a message Alt+R resume a session Ctrl+V attach a clipboard image (falls┃
> back to pasting text) selectors and completion: Up move the selection up Down move the selection ┃
> down PgUp selection page up PgDn selection page down Enter confirm the selection Esc / Ctrl+C    ┃
> close the selection list Tab path completion / accept autocomplete commands: / slash commands    ┃
> (/help, /model, /hotkeys, …) Esc close the selector / overlay first                              ┃
> type a prompt — /help for commands
Faux test model  session-18d716aa57ba12fc                              in 0 out 0 ?/8.2k  ? for help
