# lum1319-history-session-a.png — chars, 76x26 cells

===== LUM-1319 session A (write): three real prompts submitted in a real 76x26 PTY, driving the debug binary. Each submit appends one JSON row to $HOME/.pi/agent/history.jsonl. Session B (lum1319-history-session-b.json) starts a *new process* over the same home and recalls them.  |  [1] 1. idle 76x26, empty composer, nothing in the history file yet  (cursor 76,25, frame 7b4c19abd592, px a6ea60d174fc, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> type a prompt — /help for commands
Faux test model  session-18d76ee887f80116      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'

===== LUM-1319 session A (write): three real prompts submitted in a real 76x26 PTY, driving the debug binary. Each submit appends one JSON row to $HOME/.pi/agent/history.jsonl. Session B (lum1319-history-session-b.json) starts a *new process* over the same home and recalls them.  |  [2] 2. Enter submits `first prompt` — the transcript echoes it  (cursor 25,3, frame fb0c5edba319, px 1a5da5238e0d, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> first prompt
  faux-model (faux) hello




















> type a prompt — /help for commands
Faux test model  session-18d76ee887f80116      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> first prompt'

===== LUM-1319 session A (write): three real prompts submitted in a real 76x26 PTY, driving the debug binary. Each submit appends one JSON row to $HOME/.pi/agent/history.jsonl. Session B (lum1319-history-session-b.json) starts a *new process* over the same home and recalls them.  |  [3] 3. `second prompt` is submitted too  (cursor 25,5, frame 70c8502a103f, px 0df39243f675, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> first prompt
  faux-model (faux) hello
> second prompt
  faux-model (faux) hello


















> type a prompt — /help for commands
Faux test model  session-18d76ee887f80116      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> second prompt'

===== LUM-1319 session A (write): three real prompts submitted in a real 76x26 PTY, driving the debug binary. Each submit appends one JSON row to $HOME/.pi/agent/history.jsonl. Session B (lum1319-history-session-b.json) starts a *new process* over the same home and recalls them.  |  [4] 4. `third prompt` is the newest entry when the process exits  (cursor 25,7, frame f60a9c0505e7, px eec479c17b36, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> first prompt
  faux-model (faux) hello
> second prompt
  faux-model (faux) hello
> third prompt
  faux-model (faux) hello
















> type a prompt — /help for commands
Faux test model  session-18d76ee887f80116      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> third prompt'
