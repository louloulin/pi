# lum1360-chatinput-audit.png — chars, 80x26 cells

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [1] 1. idle composer at 80x26  (cursor 80,25, frame 6ab7d114deee, px 45ad8305a8f6, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [2] 2. three words typed; caret at the end of the draft  (cursor 36,23, frame 472e155f0f25, px 43ea1dd1b14e, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma▍'  — a plain draft renders with the block caret at the end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [3] 3. Ctrl+W deletes one word backward (codex, Martty and pi-ts all bind this)  (cursor 19,23, frame 847d76fcbf3e, px 4de0de07b4e2, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta ▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta ▍'  — `gamma` is gone and the caret sits where it was

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [4] 4. Alt+B hops one word left (esc-b; untouched by the LUM-1360 defect)  (cursor 14,23, frame 1cada84f88e7, px 1015ed33d06b, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha ▍beta
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha ▍beta'  — the caret stops at the start of `beta`; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [5] 5. Alt+F hops one word right — the LUM-1360 fix (before: this forked the session)  (cursor 13,23, frame 3c045c1c4356, px 687d1d5aa48c, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   reject       '/fork: no session database'  — the fork picker must NOT run: `alt+f` is the editor's forward-word chord, not `app.session.fork` (LUM-1360). This row is a hard FAIL, so the pre-fix binary fails this scenario.
  assert PASS   probe        '> alpha beta▍'  — the caret is back at the end of `beta`, one word right; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [6] 6. Ctrl+A / Ctrl+E are line-scoped, not draft-scoped  (cursor 19,23, frame 21a4aee06736, px 9d463373690a, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> ▍alpha beta gamma
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> ▍alpha beta gamma'  — Ctrl+A put the caret at the start of the only line

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [7] 7. Ctrl+K kills to the end of the line and Ctrl+Y yanks it back  (cursor 20,23, frame 1d923d6dd718, px cb558a8e5be6, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma ▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma'  — the kill ring round-trips the whole killed tail

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [8] 8. Ctrl+J opens a second draft row instead of submitting  (cursor 20,23, frame 5dc69c7210c5, px cbf96c45708c, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them



















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma
  second line▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma'  — line 1 keeps its content (Ctrl+J did not submit)
  assert PASS   probe        '  second line▍'  — line 2 is a real draft row with the caret at its end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [9] 9. double-click inside the composer selects the word under the pointer  (cursor 14,23, frame 22796c547a6c, px f95a5391243d, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them



















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma
  second ▍line
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft is intact after the double click

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [10] 10. a drag that starts in the composer extends the selection as the pointer moves left  (cursor 14,23, frame d24911e22666, px f4a83a4fd9b2, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them



















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma
  seco▍nd line
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft survives a drag that leaves the composer body

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [11] 11. Ctrl+C clears the draft on the first press (no exit)  (cursor 36,23, frame 6ab7d114deee, px 45ad8305a8f6, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> type a prompt'  — the composer is back to its placeholder and the process is alive

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [12] 12. Enter submits, the transcript echoes the prompt  (cursor 25,3, frame 6d5b378df642, px db0a3e6225c2, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  faux-model (faux) hello


















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity'  — the submitted prompt is echoed in the transcript

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [13] 13. Up on the empty composer recalls it from the prompt history  (cursor 36,23, frame ec98fa830b9b, px bf3d52a23975, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  faux-model (faux) hello


















────────────────────────────────────────────────────────────────────────────────
> hello codex parity▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity▍'  — history recall puts the prompt back in the composer

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [14] 14. Ctrl+R opens the reverse search over the prompt history  (cursor 80,22, frame 18041d175a6c, px 353da929cb6a, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> hello codex parity
  faux-model (faux) hello

















────────────────────────────────────────────────────────────────────────────────
reverse-i-search: hel  Enter accept · Esc cancel
> hello codex parity▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7989ab555e4dd          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'hello codex parity'  — the reverse search lists the matching history entry
