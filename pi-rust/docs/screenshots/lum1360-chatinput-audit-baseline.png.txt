# ab-before.png — chars, 80x26 cells

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [1] 1. idle composer at 80x26  (cursor 80,25, frame b4895983a65c, px 6298027f2fcd, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [2] 2. three words typed; caret at the end of the draft  (cursor 36,23, frame 9983c9f1d7a6, px 7f565412cbbd, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta gamma▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta gamma▍'  — a plain draft renders with the block caret at the end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [3] 3. Ctrl+W deletes one word backward (codex, Martty and pi-ts all bind this)  (cursor 19,23, frame b7a8bde35f1b, px 2a1f1e29b9a4, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha beta ▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha beta ▍'  — `gamma` is gone and the caret sits where it was

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [4] 4. Alt+B hops one word left (esc-b; untouched by the LUM-1360 defect)  (cursor 14,23, frame a9cb56cd993b, px aede943d45c3, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them




















────────────────────────────────────────────────────────────────────────────────
> alpha ▍beta
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> alpha ▍beta'  — the caret stops at the start of `beta`; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [5] 5. Alt+F hops one word right — the LUM-1360 fix (before: this forked the session)  (cursor 28,2, frame 3df6f357503f, px f8a12aec13e1, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database



















────────────────────────────────────────────────────────────────────────────────
> alpha ▍beta
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert FAIL   reject       '/fork: no session database'  — the fork picker must NOT run: `alt+f` is the editor's forward-word chord, not `app.session.fork` (LUM-1360). This row is a hard FAIL, so the pre-fix binary fails this scenario.
  assert XFAIL  probe        '> alpha beta▍'  — the caret is back at the end of `beta`, one word right; the frame trims the draft's trailing space

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [6] 6. Ctrl+A / Ctrl+E are line-scoped, not draft-scoped  (cursor 19,23, frame bbb118d61efb, px 141f456d1b89, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database



















────────────────────────────────────────────────────────────────────────────────
> ▍alpha  gammabeta
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert XFAIL  probe        '> ▍alpha beta gamma'  — Ctrl+A put the caret at the start of the only line

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [7] 7. Ctrl+K kills to the end of the line and Ctrl+Y yanks it back  (cursor 20,23, frame f4d434df4246, px c7197c5ac533, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database



















────────────────────────────────────────────────────────────────────────────────
> alpha  gammabeta ▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert XFAIL  probe        '> alpha beta gamma'  — the kill ring round-trips the whole killed tail

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [8] 8. Ctrl+J opens a second draft row instead of submitting  (cursor 20,23, frame 661816073218, px 0f4494d2b95a, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database


















────────────────────────────────────────────────────────────────────────────────
> alpha  gammabeta
  second line▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert XFAIL  probe        '> alpha beta gamma'  — line 1 keeps its content (Ctrl+J did not submit)
  assert PASS   probe        '  second line▍'  — line 2 is a real draft row with the caret at its end

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [9] 9. double-click inside the composer selects the word under the pointer  (cursor 14,23, frame c0c4d1a4226e, px 08698ff0ab42, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database


















────────────────────────────────────────────────────────────────────────────────
> alpha  gammabeta
  second ▍line
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft is intact after the double click

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [10] 10. a drag that starts in the composer extends the selection as the pointer moves left  (cursor 14,23, frame 86a5bbd65317, px 5972805656b0, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database


















────────────────────────────────────────────────────────────────────────────────
> alpha  gammabeta
  seco▍nd line
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'lpha'  — the draft survives a drag that leaves the composer body

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [11] 11. Ctrl+C clears the draft on the first press (no exit)  (cursor 36,23, frame 2d8312359274, px 1a008ea78b4c, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database



















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> type a prompt'  — the composer is back to its placeholder and the process is alive

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [12] 12. Enter submits, the transcript echoes the prompt  (cursor 25,4, frame 4c01e2a524b1, px 18902fceb577, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database
> hello codex parity
  faux-model (faux) hello

















────────────────────────────────────────────────────────────────────────────────
> type a prompt — /help for commands
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity'  — the submitted prompt is echoed in the transcript

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [13] 13. Up on the empty composer recalls it from the prompt history  (cursor 36,23, frame c708c15c19e2, px f9a18d4d5c54, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database
> hello codex parity
  faux-model (faux) hello

















────────────────────────────────────────────────────────────────────────────────
> hello codex parity▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        '> hello codex parity▍'  — history recall puts the prompt back in the composer

===== LUM-1360 chatinput A/B: the composer's word-motion and readline vocabulary, driven in a real 80x26 PTY against the real binary. Panel 5 (`Alt+F`) is the A/B pair for the LUM-1360 defect: before the fix the global `app.session.fork` binding consumed `alt+f` and the frame showed `/fork: no session database`; after it, `alt+f` is `tui.editor.cursorWordRight` and the caret hops right, matching codex `move_word_right`, Martty `WordRight` and upstream `cursorWordRight`. Composer row is y=23, prompt prefix is x=0..1.  |  [14] 14. Ctrl+R opens the reverse search over the prompt history  (cursor 80,22, frame 9176ae331c78, px 1e938973c0a9, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> /fork: no session database
> hello codex parity
  faux-model (faux) hello
















────────────────────────────────────────────────────────────────────────────────
reverse-i-search: hel  Enter accept · Esc cancel
> hello codex parity▍
────────────────────────────────────────────────────────────────────────────────
Faux test model  session-18d7976d132152a5          in 0 out 0 ?/8.2k  ? for help
  assert PASS   probe        'hello codex parity'  — the reverse search lists the matching history entry
