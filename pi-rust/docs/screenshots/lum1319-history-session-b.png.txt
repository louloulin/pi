# lum1319-history-session-b.png — chars, 76x26 cells

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [1] 1. fresh process: Up recalls the newest prompt written by the previous process  (cursor 36,24, frame 358e8a6ff77e, px 3c08733c4262, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> third prompt▍
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> third prompt'
  assert PASS   probe        '> first prompt' (x0)  — Up starts at the newest entry: the transcript is empty in this process, so the only `> third prompt` is the recalled draft

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [2] 2. Down leaves the history again and restores the empty draft  (cursor 36,24, frame 05d01ed31790, px a733e3a4559a, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> type a prompt — /help for commands
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [3] 3. Ctrl+R opens the reverse search with an empty query: no preview, no draft damage  (cursor 2,24, frame 3f3f947126d2, px 2373ef9da776, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them





















reverse-i-search:
> type a prompt — /help for commands
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'reverse-i-search:'
  assert PASS   probe        'third prompt' (x0)  — codex's Idle state: opening the search previews nothing until a query is typed

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [4] 4. typing `prompt` filters the history and previews the newest match  (cursor 36,24, frame ff2ad6a11581, px cbf4d8052c7d, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them





















reverse-i-search: prompt  Enter accept · Esc cancel
> third prompt▍
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'reverse-i-search: prompt'
  assert PASS   expect       'Enter accept'
  assert PASS   expect       'Esc cancel'
  assert PASS   expect       '> third prompt'

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [5] 5. Ctrl+R again steps to the older unique match  (cursor 16,24, frame cbe615eaa01b, px 36f57e91dad0, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them





















reverse-i-search: prompt  Enter accept · Esc cancel
> second prompt▍
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> second prompt'

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [6] 6. Ctrl+R at the oldest match keeps it (boundary, not a miss)  (cursor 16,24, frame c93c0714734e, px a058400fc6a2, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them





















reverse-i-search: prompt  Enter accept · Esc cancel
> first prompt▍
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> first prompt'
  assert PASS   expect       'no match' (x0)  — a boundary is not a miss

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [7] 7. Esc cancels the search and restores the pre-search draft  (cursor 36,24, frame 05d01ed31790, px a733e3a4559a, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them






















> type a prompt — /help for commands
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt'
  assert PASS   probe        'reverse-i-search' (x0)  — the search row is gone with the mode

===== LUM-1319 session B (recall): a second process, same $HOME. `Up` recalls `third prompt` — submitted by the *previous* process, not this one. `Ctrl+R` opens `reverse-i-search:`, typing filters to the newest match, repeated `Ctrl+R` walks to the older unique match and stops at the boundary, `Esc` restores the pre-search draft, and `Up` + `Enter` submits the recalled prompt. Real binary, real PTY, pyte VT emulator.  |  [8] 8. Up recalls and Enter submits the cross-process prompt  (cursor 25,3, frame d6d6faf3d068, px 4be6372fcf3f, alive=True) =====
pi v0.1.0
hints hidden on a short terminal — Alt+H shows them
> third prompt
  faux-model (faux) hello




















> type a prompt — /help for commands
Faux test model  session-18d76fbe1873ef54      in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> third prompt'
