# lum1267-interaction-assertions.png — chars, 120x34 cells

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [1] 1. idle: product line, hint table, onboarding line, composer placeholder, status line  (cursor 120,33, frame 9bc7f584d4e6, px 91d3c86ec24f, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.











> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'pi v0.1.0'
  assert PASS   expect       'Pi can explain its own features'
  assert PASS   expect       '  / for commands'
  assert PASS   expect       '  ! to run bash'
  assert PASS   expect       '  Alt+H to hide this header'
  assert PASS   expect       '> type a prompt — /help for commands'
  assert PASS   expect       'Faux test model'
  assert PASS   expect       '? for help'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [2] 2. type / -> the command completion opens with a live counter and the first candidate selected  (cursor 36,32, frame 9c42ce63b318, px 8d6a7269105c, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.





❯ help  Show this help text
  clear  Clear the message view
  new  Start a new session
  copy  Copy last agent message to clipboard
  name  <name> — Set session display name
  (1/19)
> /▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '❯ help'
  assert PASS   expect       'Show this help text'
  assert PASS   expect       '\\(1/\\d+\\)'  — candidate counter, first item selected

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [3] 3. type mo -> /model stays selected and the over-broad candidate set is visible (see the xfail_reject markers)  (cursor 6,32, frame 35682e1b5535, px 3c14567651a8, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.





❯ model  <provider/model> — Select model (opens selector UI)
  copy  Copy last agent message to clipboard
  thinking  off|minimal|low|medium|high|xhigh|max — Set the reasoning level
  export  [path] — Export session (HTML default, or a .jsonl path)
  fork  Create a new fork from a previous user message
  (1/8)
> /mo▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> /mo'
  assert PASS   expect       '❯ model'
  assert PASS   expect       '\\(1/\\d+\\)'  — candidate counter
  assert XFAIL  xfail_reject 'Copy last agent message to clipboard'  — LUM-1263: upstream filters candidates by command name only; matching the description too is a deviation and must go
  assert XFAIL  xfail_reject 'Create a new fork from a previous user message'  — LUM-1263: same over-broad filter

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [4] 4. Down -> the selection marker moves to the next candidate and the counter agrees  (cursor 4,31, frame 138a5f9a286a, px b73013dca01b, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.





  model  <provider/model> — Select model (opens selector UI)
❯ copy  Copy last agent message to clipboard
  thinking  off|minimal|low|medium|high|xhigh|max — Set the reasoning level
  export  [path] — Export session (HTML default, or a .jsonl path)
  fork  Create a new fork from a previous user message
  (2/8)
> /mo▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '❯ copy' (x1)
  assert PASS   expect       '\\(2/\\d+\\)'  — counter follows the marker

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [5] 5. Tab -> the selected candidate is applied to the composer and the list closes  (cursor 9,32, frame 60de79d661ba, px 5057d99600fb, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.











> /copy ▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> /copy' (x1)
  assert PASS   reject       '\\(\\d+/\\d+\\)'  — the completion list must be gone once a candidate is accepted

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [6] 6. Enter -> the slash command runs and reports its result in the transcript  (cursor 36,32, frame bbfc0446ee26, px f90a31234d8f, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet










> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> nothing to copy yet'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [7] 7. /model + Enter -> the model selector overlay lists real models with their providers  (cursor 56,31, frame 397ab42b0a18, px b93b8a7b71c6, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet
Pick a model
────────────────────────────────────────
❯ Ling 2.6 1T                     ant-ling
  Ling 2.6 Flash                  ant-ling
  Ring 2.6 1T                     ant-ling
  Claude Haiku 4.5                anthropic
  Claude Opus 4.5                 anthropic
  Claude Sonnet 4.5               anthropic
  GPT-4                           azure-openai-responses
  GPT-4 Turbo                     azure-openai-responses
> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'Pick a model'
  assert PASS   expect       'anthropic'
  assert PASS   expect       '─'  — overlay frame

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [8] 8. Esc -> the overlay closes and the composer is back to its placeholder  (cursor 56,31, frame bbfc0446ee26, px f90a31234d8f, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet










> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> type a prompt — /help for commands'
  assert PASS   reject       'Pick a model'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [9] 9. @ -> file-path completion over the fixture tree, with paths resolved relative to the cwd  (cursor 36,32, frame 1e37ca36d4c5, px a64f1979c0e8, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet





❯ src/  src
  README.md  README.md
  Cargo.toml  Cargo.toml
  lib.rs  src/lib.rs
  main.rs  src/main.rs
> @▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '❯ src/'
  assert PASS   expect       'Cargo.toml'
  assert PASS   expect       'README.md'
  assert PASS   expect       '> @'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [10] 10. src -> candidates narrow to that subtree; the unfiltered fixtures are gone  (cursor 7,32, frame 6786ffed18ea, px b9f757843c8d, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet







❯ src/  src
  lib.rs  src/lib.rs
  main.rs  src/main.rs
> @src▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> @src'
  assert PASS   reject       'Cargo.toml'
  assert PASS   reject       'README.md'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [11] 11. Tab -> the path token is composed into the composer and the list closes  (cursor 8,32, frame 72851a2433e1, px a0bfeafd1f63, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet










> @src/▍
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> @src/'
  assert PASS   reject       '❯ src/'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [12] 12. a real turn through the provider: the prompt and the reply both land in the transcript  (cursor 49,33, frame 17c98e84bfd6, px 4012d978f845, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet
> hi
  faux-model (faux) hello








> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '> hi'
  assert PASS   expect       'faux-model (faux) hello'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [13] 13. !bash -> the command, its output and its duration are reported as one tool block  (cursor 11,26, frame 61ad7617928e, px b91d0f1337f4, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet
> hi
  faux-model (faux) hello
* bash echo pty-harness-ok
* pty-harness-ok
* Took 0.0s





> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '* bash echo pty-harness-ok'
  assert PASS   expect       '* pty-harness-ok'
  assert PASS   expect       '\\* Took \\d'  — elapsed time is reported

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [14] 14. Ctrl+O -> the status line reports the tool-output expansion state  (cursor 120,33, frame 94304e2e1a29, px 16535077f8ff, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
> nothing to copy yet
> hi
  faux-model (faux) hello
* bash echo pty-harness-ok
* pty-harness-ok
* Took 0.0s





> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                       in 0 out 0 ?/8.2k  Tool output: expanded
  assert PASS   expect       'Tool output: expanded'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [15] 15. /help -> the slash-command reference renders into the transcript  (cursor 120,33, frame 4cf5134ae437, px 685103f4c1aa, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
· slash commands: /help show this help text /clear clear the message view /new start a new session /copy copy the last │
· assistant message to the clipboard /name [name] show or set the session display name /model pick a model (opens      │
· selector) /session show the current session info /export [path] export the session (HTML, or JSONL for a .jsonl path)│
· /resume resume a previous session /tree navigate the session tree and switch branches /fork branch a new session from│
· a user message /clone copy the current session into a new session file /settings show or change interface settings   ┃
· /thinking set the reasoning level (/thinking off|minimal|low|medium|high|xhigh|max) /trust show or set project trust ┃
· (/trust yes|no) /compact summarize the conversation prefix to free context /hotkeys list the keyboard shortcuts      ┃
· /extensions list loaded extensions and what they register /exit quit the interactive session keys: Enter submit promp┃
· Up / Down navigate prompt history PgUp/PgDn scroll the chat log one page Home / End jump to the start / end of the   ┃
· chat log Ctrl+C abort the current turn (or clear the prompt on idle; twice exits) Ctrl+D exit on an empty prompt     ┃
· Ctrl+L open the model selector Ctrl+U clear the prompt buffer Esc close selector / cancel turn                       ┃
> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'slash commands:'
  assert PASS   expect       '/thinking'
  assert PASS   expect       '/hotkeys'
  assert PASS   expect       '/compact'
  assert PASS   expect       'keys:'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [16] 16. /hotkeys -> the chord table renders into the transcript  (cursor 96,31, frame f2fc5b65147e, px b99d956e1b56, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
· Ctrl+Shift+Down / Ctrl+Down jump to the next prompt Ctrl+Shift+F search the chat log Enter / Ctrl+G next search hit  │
· Shift+Enter / Ctrl+Shift+G previous search hit Esc close the search bar app: Esc cancel autocomplete / abort streamin│
· Ctrl+C clear the prompt (twice: exit) Ctrl+D exit when the prompt is empty Shift+Tab cycle the thinking level Ctrl+P │
· cycle to the next model Shift+Ctrl+P cycle to the previous model Ctrl+X copy the last assistant message Alt+Enter    │
· queue a follow-up message Alt+Up restore queued messages to the editor Ctrl+T show or hide thinking blocks Ctrl+O    │
· expand or collapse tool output (Ctrl+O by default) Alt+H expand or collapse the startup header (Alt+H by default)    │
· Ctrl+L open the model selector Alt+N start a new session Alt+T open the session tree Alt+F fork a session from a     │
· message Alt+R resume a session Ctrl+V attach a clipboard image (falls back to pasting text) selectors and completion:│
· Up move the selection up Down move the selection down PgUp selection page up PgDn selection page down Enter confirm  ┃
· the selection Esc / Ctrl+C close the selection list Tab path completion / accept autocomplete commands: / slash      ┃
· commands (/help, /model, /hotkeys, …) Esc close the selector / overlay first                                         ┃
> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       'Ctrl+L open the model selector'
  assert PASS   expect       'Tab path completion / accept autocomplete'
  assert PASS   expect       'Esc close the selector / overlay first'

===== LUM-1267 interaction assertions (120x34). Run against a binary built from feature/pi.rs; every panel asserts the interaction a codex/pi-class TUI owes the user.  |  [17] 17. PgUp -> the transcript scrolls back and the jump-to-latest pill appears  (cursor 120,31, frame ac3c60f9d4cf, px b83dac968d3e, alive=True) =====
pi v0.1.0
  Esc to interrupt
  Ctrl+C to clear
  Ctrl+C twice to exit
  Ctrl+D to exit (empty)
  Ctrl+K to delete to end
  Shift+Tab to cycle thinking level
  Ctrl+P/Shift+Ctrl+P to cycle models
  Ctrl+L to select model
  Ctrl+O to expand tools
  Alt+H to hide this header
  Ctrl+T to expand thinking
  / for commands
  ! to run bash
  !! to run bash (no context)
  Alt+Enter to queue follow-up
  Alt+Up to edit all queued messages
  Ctrl+V to paste image (with text fallback)
  drop files to attach

Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.
* pty-harness-ok                                                                                                       │
* Took 0.0s                                                                                                            ┃
· slash commands: /help show this help text /clear clear the message view /new start a new session /copy copy the last ┃
· assistant message to the clipboard /name [name] show or set the session display name /model pick a model (opens      ┃
· selector) /session show the current session info /export [path] export the session (HTML, or JSONL for a .jsonl path)│
· /resume resume a previous session /tree navigate the session tree and switch branches /fork branch a new session from│
· a user message /clone copy the current session into a new session file /settings show or change interface settings   │
· /thinking set the reasoning level (/thinking off|minimal|low|medium|high|xhigh|max) /trust show or set project trust │
· (/trust yes|no) /compact summarize the conversation prefix to free context /hotkeys list the keyboard shortcuts      │
· /extensions list loaded extensions and what they register /exit quit the interactive session keys: Enter submit promp│
· Up / Down navigate prompt history PgUp/Pg ↓ Jump to latest message · End Home / End jump to the start / end of the   │
> type a prompt — /help for commands
Faux test model  session-18d727344db30b60                                                  in 0 out 0 ?/8.2k  ? for help
  assert PASS   expect       '↓ Jump to latest message'
