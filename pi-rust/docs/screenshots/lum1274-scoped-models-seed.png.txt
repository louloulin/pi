# lum1274-scoped-models-seed.png — chars, 120x34 cells

===== LUM-1274 — `/scoped-models` read-back: the SAME `settings.json` the LUM-1274 capture wrote (scene 9 of lum1274-scoped-models.json) is the child's HOME here, so a fresh start proves the persistence path end to end — the panel opens with the saved scope in the saved order (`2/192 enabled`, no `(unsaved)`) and `Ctrl+P` cycles only inside it. Real binary + real PTY + pyte, same harness as LUM-1286/1293/1294/1298/1308.  |  [1] 1. Alt+H then /scoped-models: the scope comes from settings.json at startup (2/192 enabled, in the saved order, not (unsaved))  (cursor 85,22, frame ccef304a1af1, px 4b69cb163a51, alive=True) =====

Model Configuration
────────────────────────────────────────
❯ ✓ ant-ling/Ling-2.6-flash       ant-ling
  ✓ ant-ling/Ling-2.6-1T          ant-ling
  ant-ling/Ring-2.6-1T            ant-ling
  anthropic/claude-haiku-4-5      anthropic
  anthropic/claude-opus-4-5       anthropic
  anthropic/claude-sonnet-4-5     anthropic
  azure-openai-responses/gpt-4    azure-openai-responses
  azure-openai-responses/gpt-4-t  azure-openai-responses
  (1/192)
  Enter toggle · Ctrl+A enable all · Ctrl+X clear all
  Ctrl+P toggle provider · Alt+Up / Alt+Down reorder · Ctrl+S save
  Session-only. Ctrl+S to save to settings.
  2/192 enabled
















> type a prompt — /help for commands
Ling 2.6 1T  session-18d761ca20d60635                                                      in 0 out 0 ?/262k  ? for help
  assert PASS   expect       'Model Configuration'
  assert PASS   expect       '2/192 enabled'
  assert PASS   expect       '✓ ant-ling/Ling-2.6-flash       ant-ling\n  ✓ ant-ling/Ling-2.6-1T          ant-ling'
  assert PASS   reject       '(unsaved)'
  assert PASS   reject       'all enabled'

===== LUM-1274 — `/scoped-models` read-back: the SAME `settings.json` the LUM-1274 capture wrote (scene 9 of lum1274-scoped-models.json) is the child's HOME here, so a fresh start proves the persistence path end to end — the panel opens with the saved scope in the saved order (`2/192 enabled`, no `(unsaved)`) and `Ctrl+P` cycles only inside it. Real binary + real PTY + pyte, same harness as LUM-1286/1293/1294/1298/1308.  |  [2] 2. Esc closes the panel and gives the composer back  (cursor 15,15, frame 29c30dafec28, px b05e30d97d53, alive=True) =====
































> type a prompt — /help for commands
Ling 2.6 1T  session-18d761ca20d60635                                                      in 0 out 0 ?/262k  ? for help
  assert PASS   expect       'type a prompt'
  assert PASS   reject       'Model Configuration'

===== LUM-1274 — `/scoped-models` read-back: the SAME `settings.json` the LUM-1274 capture wrote (scene 9 of lum1274-scoped-models.json) is the child's HOME here, so a fresh start proves the persistence path end to end — the panel opens with the saved scope in the saved order (`2/192 enabled`, no `(unsaved)`) and `Ctrl+P` cycles only inside it. Real binary + real PTY + pyte, same harness as LUM-1286/1293/1294/1298/1308.  |  [3] 3. Ctrl+P (app.model.cycleForward) moves to the second saved model (the scope's first entry)  (cursor 42,33, frame b19017cc787d, px 5b1164967633, alive=True) =====
> model → ant-ling/Ling 2.6 Flash































> type a prompt — /help for commands
Ling 2.6 Flash  session-18d761ca20d60635                                                   in 0 out 0 ?/262k  ? for help
  assert PASS   expect       'model → ant-ling/Ling 2.6 Flash'
  assert PASS   reject       'Ring-2.6-1T'

===== LUM-1274 — `/scoped-models` read-back: the SAME `settings.json` the LUM-1274 capture wrote (scene 9 of lum1274-scoped-models.json) is the child's HOME here, so a fresh start proves the persistence path end to end — the panel opens with the saved scope in the saved order (`2/192 enabled`, no `(unsaved)`) and `Ctrl+P` cycles only inside it. Real binary + real PTY + pyte, same harness as LUM-1286/1293/1294/1298/1308.  |  [4] 4. Ctrl+P again wraps inside the saved scope instead of walking the 192-model catalog  (cursor 42,33, frame 85d42147bcb4, px 4e13d86c2204, alive=True) =====
> model → ant-ling/Ling 2.6 Flash
> model → ant-ling/Ling 2.6 1T






























> type a prompt — /help for commands
Ling 2.6 1T  session-18d761ca20d60635                                                      in 0 out 0 ?/262k  ? for help
  assert PASS   expect       'model → ant-ling/Ling 2.6 1T'
  assert PASS   reject       'Ring 2.6 1T'
  assert PASS   reject       'anthropic/'
